msisdn:
  "+639399106689"

message:
  "Hello from AWS"

schedules:
  - task:
      type: lambda
      name: SendSMS
      handler: send_sms.handler
      environment:
        - msisdn: "+639399106689"
        - message: "Hello from AWS"
      cron:
        "cron(0 * * * ? *)"
  - task:
      type: fargate
      name: CreateReport
      command:
        - sh
        - -c
        - sleep 5 && date
      cron:
        "cron(0/1 * * * ? *)"
